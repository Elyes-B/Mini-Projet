
<div>


<div class="hero-section bg-light py-5">
    <div class="container">
        <div class="row">
            <div class="col-md-6 text-center text-md-start">
                <h1 class="display-4 mb-4">NEW ERA OF SMARTPHONES</h1>
                <div class="price-tag mb-4">
                    <span class="old-price">$530</span>
                    <span class="new-price">$460</span>
                </div>
                <h2 class="h4 mb-4">Apple iPhone 6s</h2>
                <a href="product.php?id=iphone-6s" class="btn btn-primary btn-lg">Shop Now</a>
            </div>
            <div class="col-md-6 d-flex justify-content-center align-items-center">
                <img src="assets/logo.png" class="img-fluid" style="max-height:400px;">
            </div>
        </div>
    </div>
</div>


<section class="categories-section py-5">
    <div class="container">
        <h2 class="text-center mb-4">Popular Categories</h2>
        <div class="row g-4 d-flex justify-content-center">
        <?php
        $categories = $pdo->query("SELECT cat_name, icone FROM categorie")->fetchAll();
        foreach ($categories as $category):
        ?>
            <div class="col-md-2 col-sm-4 col-6 mb-3">
                <a href="search.php?category=<?= urlencode($category['cat_name']) ?>"
                   class="category-btn d-block text-center text-decoration-none">
                    <div class="icon-wrapper mb-2">
                        <i class="<?= htmlspecialchars($category['icone']) ?>"></i>
                    </div>
                    <span><?= htmlspecialchars($category['cat_name']) ?></span>
                </a>
            </div>
        <?php endforeach; ?>
        </div>
    </div>
</section>

<section class="deals-section py-5 bg-light">
    <div class="container">
        <h2 class="mb-4">Echo Deals of the Week</h2>

        <div id="dealsCarousel" class="carousel slide">
            <div class="carousel-inner">
                <?php
                $deals = [
                    ['id'=>1,'name'=>'Beoplay H7'],
                    ['id'=>2,'name'=>'Acer Aspire 5'],
                    ['id'=>3,'name'=>'Canon EOS M50']
                ];
                foreach ($deals as $i => $deal):
                ?>
                <div class="carousel-item <?= $i === 0 ? 'active' : '' ?>">
                    <div class="row align-items-center">
                        <div class="col-md-6 text-center">
                            <img src="assets/logo.png" class="img-fluid" style="max-height:300px;">
                        </div>
                        <div class="col-md-6">
                            <h3><?= $deal['name'] ?></h3>
                            <a href="product.php?id=<?= $deal['id'] ?>" class="btn btn-primary">Shop Now</a>
                        </div>
                    </div>
                </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</section>


<section class="featured-products py-5">
    <div class="container">

 
        <ul class="nav nav-tabs justify-content-center mb-4">
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? 'new') === 'new' ? 'active' : '' ?>"
                   href="home.php?tab=new">New</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? '') === 'sale' ? 'active' : '' ?>"
                   href="home.php?tab=sale">On Sale</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? '') === 'best' ? 'active' : '' ?>"
                   href="home.php?tab=best">Best Rated</a>
            </li>
        </ul>

      
        <div class="row g-4">
        <?php if (!empty($filteredProducts)): ?>
            <?php foreach ($filteredProducts as $product): ?>
            <div class="col-md-3 col-sm-6">
                <div class="product-card text-center">

                    <?php if (!empty($product['tag'])): ?>
                        <div class="product-badge">
                            <?= implode(', ', $product['tag']) ?>
                        </div>
                    <?php endif; ?>

                    <img src="assets/logo.png"
                         class="img-fluid mb-3 mx-auto d-block"
                         style="max-width:140px;">

                    <h3 class="h6">
                        <?= htmlspecialchars($product['prd_name']) ?>
                    </h3>

                    <div class="price-section mb-3">
                        <span class="new-price">
                            $<?= $product['prd_price'] ?>
                        </span>
                    </div>

                    <div class="product-actions d-flex justify-content-center">
                        <button class="btn btn-outline-primary btn-sm me-2">
                            <i class="far fa-heart"></i>
                        </button>
                        <button class="btn btn-primary btn-sm">
                            Add to Cart
                        </button>
                    </div>

                </div>
            </div>
            <?php endforeach; ?>
        <?php else: ?>
            <p class="text-center">No products found</p>
        <?php endif; ?>
        </div>

    </div>
</section>

</div>
