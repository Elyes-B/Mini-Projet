

<div class="hero-section bg-light py-5">
    <div class="container">
        <div class="row">
            <div class="col-md-6 text-center text-md-start">
                <h1 class="display-4 mb-4">Our biggest offer</h1>
                <div class="price-tag mb-4">
                    <span class="old-price">$<?= $bestProduct['prd_price'] ?></span>
                    <span class="new-price">$<?= $bestProduct['prd_price'] - ($bestProduct['off_discount_amount'] ?? 0) ?></span>
                    <span style="font-size: 2rem;font-weight: bold"> <?= round($bestProduct['off_discount_amount'] / $bestProduct['prd_price'] * 100) ?>% off</span>
                </div>
                <h2 class="h4 mb-4"><?= $bestProduct['prd_name'] ?></h2>
                <a href="product.php?id=<?= $bestProduct['product_id'] ?>" class="btn btn-primary btn-lg">Shop Now</a>
            </div>
            <div class="col-md-6 d-flex justify-content-center align-items-center">
                <img src="<?= $bestProduct['prd_image_url'] ?>" class="img-fluid" style="max-height:400px;">
            </div>
        </div>
    </div>
</div>

<section class="categories-section py-5">
    <div class="container">
        <h2 class="text-center mb-4">Popular Categories</h2>
        <div class="row g-4 d-flex justify-content-center">
        <?php for ($i = 0; $i < count($categoris); $i++): ?>
            <div class="col-md-2 col-sm-4 col-6 mb-3">
                <a href="search.php?category=<?= $categoris[$i]['cat_name'] ?>" class="category-btn d-block text-center text-decoration-none">
                    <div class="icon-wrapper mb-2">
                        <i class="<?= $categoris[$i]['icon'] ?>"></i>
                    </div>
                    <span><?= $categoris[$i]['cat_name'] ?></span>
                </a>
            </div>
        <?php endfor; ?>
        </div>
    </div>
</section>

<section class="deals-section py-5 bg-light">
    <div class="container">
        <h2 class="mb-4">Deals of the Week</h2>

        <div id="dealsCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <?php $count = 0; foreach ($pro as $deal): ?>
                <?php if (!$deal['discount']) continue; ?>
                <div class="carousel-item <?= $count === 0 ? 'active' : '' ?>">
                    <div class="row align-items-center py-5"> <div class="col-md-6 text-center">
                            <img src="<?= $deal['prd_image_url'] ?>" class="img-fluid" style="height:300px; object-fit: contain;">
                        </div>
                        <div class="col-md-6 text-center text-md-start">
                            <h3 class="display-6 fw-bold"><?= $deal['prd_name'] ?></h3>
                            <div class="price-tag mb-4">
                    <span class="old-price">$<?= $deal['prd_price'] ?></span>
                    <span class="new-price">$<?= $deal['prd_price'] - ($deal['discount'] ?? 0) ?></span>
                        </div>
                            <a href="product.php?id=<?= $deal['product_id'] ?>" class="btn btn-primary btn-lg">Shop Now</a>
                        </div>
                    </div>
                </div>
                <?php $count++; endforeach; ?>
            </div>

            <button class="carousel-control-prev" type="button" data-bs-target="#dealsCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon bg-dark rounded-circle" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#dealsCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon bg-dark rounded-circle" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
</section>


<section class="featured-products py-5">
    <div class="container">

 
        <ul class="nav nav-tabs justify-content-center mb-4">
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? 'new') === 'new' ? 'active' : '' ?>"
                   href="home.php?tab=new">New</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? '') === 'sale' ? 'active' : '' ?>"
                   href="home.php?tab=sale">On Sale</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= ($_GET['tab'] ?? '') === 'best' ? 'active' : '' ?>"
                   href="home.php?tab=best">Best Rated</a>
            </li>
        </ul>

      
        <div class="row g-4">
        <?php if (!empty($filteredProducts)): ?>
            <?php foreach ($filteredProducts as $product): ?>
               
            <div class="col-md-3 col-sm-6">
                 <a href="product.php?id=<?= $product['product_id'] ?>">
                <div class="product-card text-center">

                    <?php if (!empty($product['tag'])): ?>
                        <div class="product-badge">
                            <?= implode(', ', $product['tag']) ?>
                        </div>
                    <?php endif; ?>

                    <img src="<?= $product['prd_image_url'] ?>" alt="<?= $product['prd_name'] ?>"
                         class="img-fluid mb-3 mx-auto d-block"
                         style="height:200px; object-fit: contain;">

                    <h3 class="h6">
                        <?= $product['prd_name'] ?>
                    </h3>

                    <div class="price-section mb-3">
                        <?php if (isset($product['discount'])): ?>
                             <span class="old-price">$<?= $product['prd_price'] ?></span>
                                <span class="new-price">$<?= $product['prd_price'] - ($product['discount'] ?? 0) ?></span>
                        <?php else: ?>
                        <span class="new-price">
                            $<?= $product['prd_price'] ?>
                        </span>
                        <?php endif; ?>
                    </div>

                    <div class="product-actions d-flex justify-content-center">
                        <form method="POST" action="wishlist.php">
                            <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                            <input type="hidden" name="action" value="wishlist">
                            <button type="submit" class="btn btn-outline-danger btn-sm me-2">
                            <i class="far fa-heart"></i>
                        </button>
                        </form>
                        
                        <?php  if (isset($product['prd_stock']) && $product['prd_stock'] > 0): ?>
                                            <form method="POST" action="cart.php">
                                             <input type="hidden" name="action" value="cart">
                                            <input type="hidden" name="product_id" value="<?php echo $product['product_id']; ?>">
                                            <input type="hidden" name="quantity" value="1">
                                            <button class="btn btn-sm btn-outline-primary"><i class="fas fa-shopping-cart"></i></button>
                                            </form>
                                            <?php else: ?>
                                                <button class="btn btn-sm btn-outline-secondary" disabled>Out of Stock</button>
                                            <?php endif; ?>
                    </div>

                </div>
                </a>
            </div>
            
            <?php endforeach; ?>
        <?php else: ?>
            <p class="text-center">No products found</p>
        <?php endif; ?>
        </div>

    </div>
</section>

