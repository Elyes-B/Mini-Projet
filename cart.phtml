

<section class="cart-page py-5">
    <div class="container">
        <h1 class="mb-4">Shopping Cart</h1>
        
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Product</th>
                                <th>Price</th>
                                <th>Quantity</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($products as $product): ?>
                            <tr>
                                <form method="POST" action="cart.php">
                                    <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                                    <input type="hidden" name="action" value="remove">
                                
                                <td>
                                    <a class="d-flex align-items-center text-decoration-none text-dark" href="product.php?id=<?= $product['product_id'] ?>">
                                            <img src="<?= $product['prd_image_url'] ?>" alt="Product Image" style="width: 60px; height: 60px; object-fit: contain;" class="me-3">
                                        <span><?= $product['prd_name'] ?></span>
                                    </a>
                                </td>
                                <td>
                                    <?php  if (isset($product['discount'])) {?>
                                                    <span class="text-decoration-line-through text-muted"><?php echo $product['prd_price']; ?></span>
                                                <?php }
                                                else {
                                                ?>
                                                    <span><?php echo $product['prd_price']; ?></span>
                                                <?php } ?>
                                                <span class="text-danger fw-bold"><?php echo isset($product['discount']) ? '$' . ($product['prd_price'] - $product['discount']) : ''; ?></span>
                                </td>
                                <td><?= $product['cart_quantity'] ?></td>
                                <td><button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button></td>
                                </form>
                            </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Order Summary</h5>
                        <hr>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Subtotal:</span>
                            <span>$<?= $total ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Average Price:</span>
                            <span>$<?= $avgPrice ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Number of items:</span>
                            <span><?= $numberOfItems ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Shipping:</span>
                            <span>$10.00</span>
                        </div>
                        <div class="d-flex justify-content-between mb-3">
                            <span>Tax:</span>
                            <span>$32.80</span>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between mb-4">
                            <strong>Total:</strong>
                            <strong>$<?= $totalAfterShipping ?></strong>
                        </div>
                        <a class="btn btn-primary btn-lg w-100 mb-2" <?php if ($totalAfterShipping == 0): ?>disabled<?php endif; ?> data-bs-toggle="modal" data-bs-target="#checkoutModal">Proceed to Checkout</a>
                        <a href="search.php" class="btn btn-outline-secondary btn-lg w-100">Continue Shopping</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="checkoutModal" tabindex="-1" aria-labelledby="checkoutModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="checkoutModalLabel">Checkout</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <?php if (!empty($products)): ?>

                    <div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <h3 class="mb-4">Shipping and Contact Information</h3>
            <form action="checkout.php" method="POST">

                <div class="row g-3 mb-4">
                    <div class="col-12">
                        <label for="address" class="form-label">Street Address</label>
                        <input type="text" class="form-control" id="address" name="street_address" placeholder="1234 Main St" required>
                    </div>
                    
                    <div class="col-md-5">
                        <label for="country" class="form-label">Country</label>
                        <select class="form-select" id="country" name="country" required>
                            <option>United States</option>
                            <option>Canada</option>
                            <option>Tunisia</option>
                            <option>France</option>
                            </select>
                    </div>
                    
                    <div class="col-md-4">
                        <label for="city" class="form-label">City</label>
                        <input type="text" class="form-control" id="city" name="city" required>
                    </div>

                    <div class="col-md-3">
                        <label for="zip" class="form-label">Zip Code</label>
                        <input type="text" class="form-control" id="zip" name="zip_code" required>
                    </div>
                </div>
                    <input type="hidden" name="client_id" value="<?php echo $_SESSION['client_id']; ?>">
                    <input type="hidden" name="action" value="checkout">
                <button class="w-100 btn btn-primary btn-lg mt-3" type="submit">Confirm purchase</button>
            </form>
        </div>
    </div>
</div>
                    
                <?php else: ?>
                    <p>Your cart is empty. Please add items to proceed to checkout.</p>
                <?php endif; ?>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
</section>


