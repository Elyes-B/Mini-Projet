

<section class="cart-page py-5">
    <div class="container">
        <h1 class="mb-4">Shopping Cart</h1>
        
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Product</th>
                                <th>Price</th>
                                <th>Quantity</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($products as $product): ?>
                            <tr>
                                <form method="POST" action="cart.php">
                                    <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                                    <input type="hidden" name="action" value="remove">
                                
                                <td>
                                    <div class="d-flex align-items-center">
                                        <img src="<?= $product['prd_image_url'] ?>" alt="Product" style="max-width: 50px;" class="me-3">
                                        <span><?= $product['prd_name'] ?></span>
                                    </div>
                                </td>
                                <td>$<?= $product['prd_price'] ?></td>
                                <td><?= $product['cart_quantity'] ?></td>
                                <td><button type="submit" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button></td>
                                </form>
                            </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Order Summary</h5>
                        <hr>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Subtotal:</span>
                            <span>$<?= $total ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Average Price:</span>
                            <span>$<?= $avgPrice ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Number of items:</span>
                            <span><?= $numberOfItems ?></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Shipping:</span>
                            <span>$10.00</span>
                        </div>
                        <div class="d-flex justify-content-between mb-3">
                            <span>Tax:</span>
                            <span>$32.80</span>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between mb-4">
                            <strong>Total:</strong>
                            <strong>$<?= $totalAfterShipping ?></strong>
                        </div>
                        <a href="checkout.php" class="btn btn-primary btn-lg w-100 mb-2" <?php if ($totalAfterShipping == 0): ?>disabled<?php endif; ?>>Proceed to Checkout</a>
                        <a href="search.php" class="btn btn-outline-secondary btn-lg w-100">Continue Shopping</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


