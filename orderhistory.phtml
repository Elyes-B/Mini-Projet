<div class="container">
    <h1 class="my-4">Order History</h1>
    <?php if (empty($orders)): ?>
        <p>You have no orders yet.</p>
    <?php else: ?>
        <div class="row g-4"> 
            <?php foreach ($orders as $order): ?>
                <div class="col-12 col-md-6 col-lg-4 d-flex">
                    <a href="orderdetails.php?order_id=<?= $order['order_id'] ?>" class="text-decoration-none w-100">
                        
                        <button type="submit" class="btn p-0 border-0 text-start w-100">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <h5 class="card-title">Order Number: <?= $order['order_id'] ?></h5>
                                    <h6 class="card-subtitle mb-2 text-body-secondary">
                                        Order Date: <?= $order['order_date'] ?>
                                    </h6>
                                    <p class="card-text">Items in order: <?= $order['item_count'] ?></p>
                                    <p class="card-text fw-bold <?php 
                                        if($order['order_status'] == 'arrived') echo 'text-success';
                                        elseif($order['order_status'] == 'canceled') echo 'text-warning';
                                        else echo 'text-danger';
                                    ?>">
                                        Status: <?= ucfirst($order['order_status']) ?>
                                    </p>
                                </div>
                            </div>
                        </button>
            </a>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>
</div>