

<section class="wishlist-page py-5">
    <div class="container">
        <h1 class="mb-4">My Wishlist</h1>
        
        <div class="row g-4">
            <?php foreach ($products as $product): ?>
            <div class="col-md-3 col-sm-6">
                <div class="card h-100">
                    <a class="text-decoration-none text-dark" href="product.php?id=<?= $product['product_id'] ?>">
                    <img src="<?= $product['prd_image_url'] ?>" class="card-img-top" alt="Wishlist Item" style="max-height: 200px; object-fit: contain;">
                    <div class="card-body">
                        <h6 class="card-title"><?= $product['prd_name'] ?></h6>
                        <?php  if (isset($product['discount'])) {?>
                                                    <span class="text-decoration-line-through text-muted"><?php echo $product['prd_price']; ?></span>
                                                <?php }
                                                else {
                                                ?>
                                                    <span><?php echo $product['prd_price']; ?></span>
                                                <?php } ?>
                                                
                                                
                                                <span class="text-danger fw-bold"><?php echo isset($product['discount']) ? '$' . ($product['prd_price'] - $product['discount']) : ''; ?></span>
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            
                        </div>
                    </a>
                        <div class="d-flex justify-content-center gap-2">
                            <form method="POST" action="cart.php">
                                <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                                <input type="hidden" name="action" value="cart">
                                <input type="hidden" name="quantity" value="1">
                                <button type="submit" class="btn btn-primary btn-sm">Add to Cart</button>
                            </form>
                            
                            <form method="POST" action="wishlist.php">
                                <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                                <input type="hidden" name="action" value="remove">
                                <button type="submit" class="btn btn-outline-danger btn-sm"><i class="fas fa-trash"></i> Remove</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <?php endforeach; ?>
        </div>
    </div>
</section>


